# Continues

Timestamped session handoffs for pickup on another machine or after `git pull` (Herm D007).

The live index is **frontmatter-driven**: run `/pck` to pick up, or `/hnd list` to
browse. This README is a static overview and is NOT auto-maintained; the handoff
files in `continues/` (and their YAML frontmatter) are canonical.
