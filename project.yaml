# Machine-readable self-description for this *-meta workspace.
# Agents (catch-up, deploys) read this before prose docs.
# Schema: see herm docs/herm/meta-project-config.md

name: Invoice
slug: invoice
group: internal
slack_channel: "#ai-psychosis"

lanes:
  invoicedaddy:
    order: 0
    role: planner
    description: Cross-project invoice assembly — plan, decompose, track, close
    model: deepseek-v4-pro
    thinking: high
    mailbox: open
    work: >-
      Poll ALL project goal boards (goals/ in each project-meta repo via
      brane MCP) and assemble monthly invoice data. Plan, decompose, and
      track invoice work; side-channel with project agents for
      billing-relevant activity. Track status/priority/next-action.
      Close the loop when an invoice ships or parks. Idle when nothing
      actionable.
  hackdaddy:
    order: 1
    role: builder
    description: Executes invoice work — data assembly, reports, exports
    model: deepseek-v4-flash
    thinking: low
    mailbox: open
    work: >-
      Poll handoffs routed to hackdaddy. Read the handoff, execute the
      work (invoice data assembly, wrklogr reports, exports), verify with
      evidence, report back. Idle when nothing routed.
