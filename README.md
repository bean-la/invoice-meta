# invoice-meta

Herm internal project — **cross-project invoice assembly**.

Purpose: poll all project goal boards (via brane MCP) and assemble monthly
invoice data. Plans, decomposes, and tracks invoice work; side-channels with
project agents for billing-relevant activity.

See `project.yaml` for lane definitions and `projects.yml` (herm-meta) for
cross-project routing topology.
