
# wrklogr tokens — PRIVATE, never commit
.env
.env.wrklogr
.hrm/
.pi/

# Generated MCP config (local / hrm upgrade); never commit
.mcp.json

# Cursor: keep guest rules in git; ignore other local Cursor state
.cursor/*
!.cursor/rules/
!.cursor/rules/**
